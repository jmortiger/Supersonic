using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class MenuItems
{
	//[MenuItem("CONTEXT/Rect Transform/Align With Anchor Min")]
 //   private static void AlignRectTranform(MenuCommand command)
	//{
	//	var rTransform = (RectTransform)command.context;
	//	//if (rTransform.anchorMin == rTransform.anchorMax &&
	//	//rTransform.
	//	var anchoredPos = rTransform.anchoredPosition;
	//	//if (rTransform.anchorMin.x == .5)
	//	//anchoredPos.x = 0;
	//	//anchoredPos.x = ((rTransform.anchorMin.x > .5) ? -1 : 1) * rTransform.pivot.x
	//	switch (rTransform.pivot.x)
	//	{
	//		case .5f:
	//			if (rTransform.anchorMin.x == .5)
	//				anchoredPos.x = 0;
	//			else
	//				anchoredPos.x = ((rTransform.anchorMin.x > .5) ? -1 : 1) * rTransform.pivot.x * rTransform.
	//			//anchoredPos.x = rTransform
	//			break;
	//		default:
	//			break;
	//	}
	//}
}
